<%= form_for([@current_user, @fridge, @fridge.fridge_items.build]) do |f| %>
  <%= f.text_field :name, placeholder: "Enter new fridge item", :required => true %>
  <%= f.date_select(:use_by_date, :order => [ :day, :month, :year], :required => true) %>
  <br>
  <%= f.submit %>
<% end %>
